---
language: node_js

node_js:
  - "0.12"

sudo: false

cache:
  directories:
    - node_modules

matrix:
  fast_finish: true

install:
  - npm install -g broccoli-cli
  - npm install -g mocha
  - npm install

script:
  - broccoli build dist && mocha tests
